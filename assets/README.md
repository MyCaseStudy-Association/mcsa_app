# Assets

This folder keeps app assets grouped by purpose.

- `icons/` - native app icon, adaptive icon, favicon, and splash images.
- `images/` - raster images used by React Native screens and components.
- `svgs/` - editable vector sources for generated brand assets.

Run `node scripts/generate-brand-assets.js` after changing files in `svgs/`.
